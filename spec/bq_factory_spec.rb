require 'spec_helper'

describe BqFactory do
  it 'has a version number' do
    expect(BqFactory::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
